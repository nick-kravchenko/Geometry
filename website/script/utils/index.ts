import { clamp } from './clamp';
import { debounce } from './debounce';
import { getCoordsByMouseEvent } from './get-coordinates-by-mouse-event';
import { setCanvasToFullScreen } from './set-canvas-to-fullscreen';

export {
  clamp,
  debounce,
  getCoordsByMouseEvent,
  setCanvasToFullScreen,
};
